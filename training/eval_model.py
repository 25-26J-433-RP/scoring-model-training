# training/eval_model.py

def evaluate_phase():
    """
    Evaluation cannot run yet because the model is not trained
    and teacher scores are not available.

    This placeholder ensures your pipeline is ready for Phase 6.
    """

    raise RuntimeError("❗ Evaluation unavailable — waiting for trained model + teacher labels.")
